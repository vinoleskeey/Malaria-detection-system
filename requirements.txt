flask==3.1.3
flask-sqlalchemy==3.1.1
sqlalchemy==2.0.36
werkzeug==3.1.5

# tensorflow was never listed here despite malaria_model/malaria_predict.py
# importing it (tensorflow.keras) - every prediction on Railway would have
# crashed with ModuleNotFoundError. numpy is bumped to 2.x to match what
# this tensorflow version actually needs (1.26.4 predates its numpy 2.x
# requirement) - versions below are what was verified working end-to-end
# against the real model, not just guessed.
tensorflow==2.20.0
numpy==2.4.3
pillow==12.1.1

# Same gap as tensorflow above: services/whisper_service.py imports
# faster_whisper for the chatbot's voice-input feature (/voice/transcribe),
# but it was never listed here either - that route would crash on Railway.
faster-whisper==1.2.1

gunicorn==23.0.0
psycopg2-binary==2.9.9
requests==2.32.3

python-dotenv==1.0.1

# PDF diagnostic report generation (pdf_utils.py) - pure-Python, no system
# libs needed (unlike wkhtmltopdf/weasyprint), so it installs cleanly on
# Railway's Nixpacks build the same way it does locally.
reportlab==5.0.1

# Background scheduler for appointment reminder emails (send_appointment_reminders
# in app_fixed.py) - pure-Python, no external broker (Redis/Celery) needed since
# this app runs a single gunicorn worker.
apscheduler==3.11.3
